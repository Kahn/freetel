language: c

matrix:
    include:
        - os: linux
          sudo: required
          env:
            - PACKAGE=freedv-dev
        - os: osx
          env:
            - PACKAGE=freedv-dev
        - os: linux
          sudo: required
          env:
            - PACKAGE=freedv
        - os: osx
          env:
            - PACKAGE=freedv

script: cd $PACKAGE && cmake - && make
